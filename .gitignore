node_modules/
dist/
*.tsbuildinfo
.DS_Store
.claude/
docs/*
!docs/samples/
# Internal-only engineering notes — intentionally NOT committed.
# (docs/* already ignores these; listed explicitly so the intent is obvious.)
docs/known-issues/
*.pdf

